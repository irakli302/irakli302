#include <iostream>
using namespace std;

int main(){
    int a, b, c, T;
    cin >> T;
    while (T--)
    {
      cin >> a >> b >> c;  
      if (a + b == c) cout << "+" << endl;
        else if (a - b == c) cout << "-" << endl;
    }
}
