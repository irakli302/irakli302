#include <iostream>
using namespace std;

int main(){
    int n,k,count1=0,count2;
    cin>>n;
    for(int i=0;i<n;++i){
        cin>>k;
        if(k>0)count1+=k;
            else {
                if(count1>0)count1-=1;
                    else count2+=1;
            }
    }
    cout<<count2;
}
