#include <iostream>
using namespace std;

int main(){
    int t;
    cin >> t;
    while (t--)
    {
        string s;
        cin >> s;
        cout << s[0];
        int N = s.size();
        for (int i = 1; i < N; i+=2)
        {
            cout << s[i];
        }
        cout << endl; 
    }
}
