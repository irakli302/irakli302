#include <iostream>
using namespace std;

int main(){
    string s;
    int T;
    cin>>T;
    while(T--){
        cin>>s;
        for(int i=0;s[i];++i) s[i]=toupper(s[i]);
        if(s=="YES")cout<<"YES"<<endl;
            else cout<<"NO"<<endl;
    }
}
